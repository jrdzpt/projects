1. Create the Databas
CREATE DATABASE IF NOT EXISTS consumer_db;
USE consumer_db;


2. Create the Tables
-- 1. Table for Administrative Users
CREATE TABLE IF NOT EXISTS users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL
);

-- 2. Table for Active Consumers
CREATE TABLE IF NOT EXISTS consumers (
    id INT AUTO_INCREMENT PRIMARY KEY,
    accountnumber VARCHAR(8) NOT NULL UNIQUE,
    first_name VARCHAR(100),
    last_name VARCHAR(100),
    middle_name VARCHAR(100),
    gender VARCHAR(20),
    birthdate DATE,
    address TEXT,
    contact_number VARCHAR(20),
    email VARCHAR(100),
    photo VARCHAR(255),
    SerialNumber VARCHAR(100),
    MeterBrand VARCHAR(100),
    status VARCHAR(50),
    present_reading DECIMAL(10,2) DEFAULT 0.00,
    previous_reading DECIMAL(10,2) DEFAULT 0.00,
    kilowatthour DECIMAL(10,2) DEFAULT 0.00
);

-- 3. Table for Archived Records
-- This mirrors the consumer table but adds the archived_at timestamp
CREATE TABLE IF NOT EXISTS archive (
    id INT PRIMARY KEY, -- We keep the original ID from consumers table
    accountnumber VARCHAR(8),
    first_name VARCHAR(100),
    last_name VARCHAR(100),
    middle_name VARCHAR(100),
    gender VARCHAR(20),
    birthdate DATE,
    address TEXT,
    contact_number VARCHAR(20),
    email VARCHAR(100),
    photo VARCHAR(255),
    SerialNumber VARCHAR(100),
    MeterBrand VARCHAR(100),
    status VARCHAR(50),
    present_reading DECIMAL(10,2),
    previous_reading DECIMAL(10,2),
    kilowatthour DECIMAL(10,2),
    archived_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

3. Create your first Admin Account
INSERT INTO users (username, password) 
VALUES ('admin', 'admin123');


